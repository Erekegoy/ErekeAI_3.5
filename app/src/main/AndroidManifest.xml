<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:usesCleartextTraffic="true"
        android:name=".ErekeAiApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.ErekeAI"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules">

        android:networkSecurityConfig="@xml/network_security_config"

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ErekeAI"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ MCP Server: другие MCP-клиенты (Claude Desktop и т.п.) в той же сети видят
             инструменты ErekeAI как свои собственные tools. Запускается явно из Настроек. -->
        <service
            android:name="com.erekeai.server.ErekeMcpServerService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- 🟡 SDK/API — REST-ядро для других ваших приложений, отдельно от MCP-сервера выше. -->
        <service
            android:name="com.erekeai.server.ErekeApiForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- 🟡 Планировщик фоновых агентов использует свою Hilt-конфигурацию WorkManager
             (ErekeAiApplication implements Configuration.Provider) — стандартный автозапуск
             WorkManager через androidx-startup нужно отключить, иначе будет конфликт. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
